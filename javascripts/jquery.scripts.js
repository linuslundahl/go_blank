(function ($) {
  /**
   * Drupal behaviors
   */
  Drupal.behaviors.GO_BLANK = {
    attach: function(context, settings) {
      // Do something?
    }
  };

  /**
   * Document ready
   */
  $(function () {
    // Do something?
  });
}(jQuery));